package collections;

public interface p {
	//interface:generic blueprint of class
	//adstract:concrete implmentation(common implemtatation logic for diff class)
	//class:concrete bluprint which holds data and logic together
//set:collection never contains duplicate
	//list:list is index based and duplicates are allowed
	//queue:it works on concept if fifo
	//deque is a double ended queue where elements are inserted and removed
}
